Put raw popover captures here as `01.png`, `02.png` ... then run
`python3 design/make_screenshots.py`.

Capture with **Cmd-Shift-4, then Space**, then click the popover. That grabs the
window with its shadow and a transparent background, which is exactly what the
compositor wants.

What each number should show is listed in `design/SCREENSHOTS.md`.
